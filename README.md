# FastAPIProject

Repo for a FastAPI web app.

## Run with Docker

Build and start the app:

```powershell
docker compose up --build
```

Then open `http://127.0.0.1:8000`.

Stop the container with `Ctrl+C`.
